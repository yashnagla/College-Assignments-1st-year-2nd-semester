#include<stdio.h>
int main()
{
	int num;
	printf("\n Enter an integer:");
	scanf("%d",&num);
	//TRUE IF NUM IS PERFECTLY DIVISIBLE BY 2
	if(num%2==0)
	    printf("%d is even",num);
	else
	    printf("%d is odd",num);
	return 0;
}
